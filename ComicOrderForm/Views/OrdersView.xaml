<Window x:Class="ComicOrders.WPF.Views.OrdersView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ComicOrderForm"
        mc:Ignorable="d"
        Title="Comic Orders" Height="350" Width="525">
    <DockPanel>
        <ScrollViewer>
            <StackPanel DockPanel.Dock="Left" Orientation="Vertical" Margin="2">
                <ComboBox Margin="2" ItemsSource="{Binding OrderDates}" SelectedItem="{Binding SelectedItem}"/>
                <Button Content="View Customers"/>
                <Button Content="View Comics"/>
                <Button Content="Add Order"/>
                <Button Content="Remove Selected Orders"/>
                <Button Content="Export Order"/>
            </StackPanel>
        </ScrollViewer>
        <DataGrid ItemsSource="{Binding Orders}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Comic" Binding="{Binding Comic}" />
                <DataGridTextColumn Header="Customer" Binding="{Binding Customer}" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
