<Window x:Class="ComicOrders.WPF.Views.CustomersView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ComicOrders.WPF.Views"
        mc:Ignorable="d"
        Title="Customers" Height="450" Width="800" Style="{DynamicResource DefaultWinStyle}">
    <DockPanel>
        <ScrollViewer DockPanel.Dock="Left">
            <StackPanel >
                <Button Content="Add Customer" Command="{Binding AddCustomer}"/>
                <Button Content="Edit Selected Customer" Command="{Binding EditSelectedCustomer}"/>
                <Button Content="Delete Selected Customers" Command="{Binding DeleteSelectedCustomers}"/>
            </StackPanel>
        </ScrollViewer>
        <DataGrid DockPanel.Dock="Right" ItemsSource="{Binding Customers}" CanUserAddRows="False" CanUserDeleteRows="False" SelectedItem="{Binding SelectedCustomer}" IsReadOnly="True">
            <!--<DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding FirstName}" Header="First Name"/>
                <DataGridTextColumn Binding="{Binding LastName}" Header="Last Name"/>
                <DataGridTextColumn Binding="{Binding Email}" Header="Email"/>
            </DataGrid.Columns>-->
        </DataGrid>
    </DockPanel>
</Window>
